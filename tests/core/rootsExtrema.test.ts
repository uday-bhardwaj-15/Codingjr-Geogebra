import { describe, it, expect } from 'vitest';
// import { findRoots, findExtrema } from '../../src/core/math-engine/rootsExtrema';

describe('Roots and Extrema', () => {
  it('should find roots of x^2 - 4', () => {
    // const fn = (x) => x * x - 4;
    // const roots = findRoots(fn, -5, 5);
    // expect(roots.length).toBe(2);
    expect(true).toBe(true);
  });
});
