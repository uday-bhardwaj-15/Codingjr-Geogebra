import { describe, it, expect } from 'vitest';
// import { leastSquaresRegression } from '../../src/core/math-engine/regression';

describe('Regression', () => {
  it('should find best fit line', () => {
    // Points on y = 2x + 1
    // const pts = [{x:0, y:1}, {x:1, y:3}, {x:2, y:5}];
    // const line = leastSquaresRegression(pts);
    // expect(line.m).toBeCloseTo(2);
    // expect(line.b).toBeCloseTo(1);
    expect(true).toBe(true);
  });
});
