import { describe, it, expect } from 'vitest';
// import { tangentsToCircle } from '../../src/core/geometry/tangents';

describe('Tangents', () => {
  it('should find two tangents from external point to circle', () => {
    // const c = { center: { x: 0, y: 0 }, radius: 1 };
    // const pt = { x: 2, y: 0 };
    // const lines = tangentsToCircle(pt, c);
    // expect(lines.length).toBe(2);
    // TODO: implement tangents and test
    expect(true).toBe(true);
  });
});
