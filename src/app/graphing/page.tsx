import { CalculatorShell } from '../../components/shell/CalculatorShell';

export const metadata = {
  title: 'Graphing Calculator - GraphKit',
  description: 'Interactive Graphing Calculator',
};

export default function GraphingPage() {
  return <CalculatorShell />;
}
